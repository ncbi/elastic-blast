[cloud-provider]
gcp-project = some-project
gcp-region = test-gcp-region
gcp-zone = some-zone

[blast]
program = blastn
db = gs://some-bucket/some-db
queries = test-queries
results = gs://user-bucket
